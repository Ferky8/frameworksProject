<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="es.deusto.mybatis.example.ElementoMapper">

	<select	id="getElementoPorId" parameterType="int" resultType="Elemento">
		SELECT * FROM ELEMENTOS WHERE IDELEMENTO=#{id}
	</select>

</mapper>
